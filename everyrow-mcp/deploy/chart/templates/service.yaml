apiVersion: v1
kind: Service
metadata:
  name: everyrow-mcp
spec:
  selector:
    app: everyrow-mcp
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP
