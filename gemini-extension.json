{
  "name": "futuresearch",
  "description": "Gemini extension for FutureSearch: AI forecasting (probabilities, dates, numbers) plus dataset research, ranking, and dedupe tools.",
  "version": "0.22.0",
  "mcpServers": {
    "futuresearch": {
      "command": "uvx",
      "args": ["futuresearch-mcp"],
      "env": {
        "FUTURESEARCH_API_KEY": "$FUTURESEARCH_API_KEY"
      }
    }
  }
}
