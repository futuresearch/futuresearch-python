{
  "name": "everyrow",
  "description": "Gemini extension for the everyrow SDK - AI-powered data processing utilities for transforming, deduping, merging, ranking, and classifying dataframes",
  "version": "0.5.1",
  "mcpServers": {
    "everyrow": {
      "command": "uvx",
      "args": ["everyrow-mcp"],
      "env": {
        "EVERYROW_API_KEY": "$EVERYROW_API_KEY"
      }
    }
  }
}