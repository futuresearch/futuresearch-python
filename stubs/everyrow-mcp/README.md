# everyrow-mcp

This package has been renamed to [futuresearch-mcp](https://pypi.org/project/futuresearch-mcp/).

```
pip install futuresearch-mcp
```

This stub remains for backwards compatibility. The `everyrow-mcp` CLI command continues to work.
